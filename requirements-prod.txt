# Production requirements: extends the base set with PostgreSQL, Redis, gunicorn, and web3.
# Install with: pip install -r requirements-prod.txt
-r server/requirements.txt
psycopg2-binary>=2.9.9
redis>=5.0.0
gunicorn>=21.2.0
web3>=6.20.0
